/**
 * 数据访问层聚合出口（barrel）
 * 按职责拆分到子模块，此处仅 re-export，保持调用方 `import * as 库` 兼容：
 * - 采集写入:  采集写入.ts（任务启用/保存视频/评论/动态/情感/日志/未分析查询）
 * - 任务管理:  任务管理.ts（任务 CRUD / 清空评论 / 删除情感）
 * - 查询:      查询.ts（列表分页 / 计数 / 统计概览 / 情感分布与趋势 / 日志统计）
 * - 配置:      配置.ts（系统配置键值对 + 缓存）
 * - AI提供者:  AI提供者.ts（LLM 服务商 CRUD + 默认提供者）
 */

export * from "./采集写入";
export * from "./任务管理";
export * from "./查询";
export * from "./配置";
export * from "./AI提供者";
