{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --target bun --outdir dist",
    "start": "bun run dist/index.js",
    "type:check": "tsc --noEmit",
    "db:推送": "drizzle-kit push",
    "db:生成": "drizzle-kit generate",
    "db:查看": "drizzle-kit studio"
  },
  "dependencies": {
    "@renmu/bili-api": "^2.14.0",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.7.0",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^26.1.1",
    "better-sqlite3": "^12.11.1",
    "drizzle-kit": "^0.31.10",
    "typescript": "^7.0.2"
  }
}
