{
  "name": "bili-opinion-monitor",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:server": "bun --filter server dev",
    "dev:client": "bun --filter client dev",
    "dev": "concurrently -n 服务端,前端 -c blue,green \"bun run dev:server\" \"bun run dev:client\"",
    "build": "bun run --filter server build && bun run --filter client build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
